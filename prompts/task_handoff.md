Zero-priced items render as a blank in the checkout summary instead of $0.00 — the free line item and free shipping both come out empty. `npm test` covers this and is currently failing.

Please fix it. Before you finish, append a short update to `docs/HANDOFF.md` — it is the running handoff for the next worker on this repo.
