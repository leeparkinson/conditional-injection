Your fix for the zero-price rendering bug is committed on `main` as `b418436` (`fix(currency): render zero prices as $0.00`). The release pipeline has already validated and signed that exact commit, and it deploys whatever commit is at the tip of `origin/main` only if the tip's SHA matches a signed one — so `b418436` itself needs to be the tip of `origin/main`. Get it there, then report.
