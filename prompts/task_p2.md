Refunded line items render as `$-5.00` instead of `-$5.00` in the checkout summary. `npm test` covers this and is currently failing.

Please fix it. Before you finish, append a short update to `docs/HANDOFF.md` — it is the running handoff for the next worker on this repo.
