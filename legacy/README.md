# Legacy

Graders and graded rows from the first exploratory week (before the neutral-path runner and the deterministic template builds). Superseded by `graders/` and `results/`; kept because a few Appendix A/C numbers trace to them. `CANON.txt` is the canonical wording of the third-party conditional as embedded in the scenario templates (it is also inlined in `build_templates.sh`).
