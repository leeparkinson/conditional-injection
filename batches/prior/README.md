# Earlier-run batch drivers (provenance only)

The drivers, job lists and logs of the exploratory phase and the first full pass (2026-08-28 to 2026-09-02; Appendix C of the paper), kept verbatim. They assumed the repository layout of that time (`run_bland.sh` at the root, config in `cfg/`) and are not meant to be re-run; the reported cells are replayed by `../run_cells.sh` from `../cells.tsv`.
